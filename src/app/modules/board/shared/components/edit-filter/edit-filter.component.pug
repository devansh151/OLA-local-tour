mat-form-field
	input('matInput'="",placeholder="Filter name",'required'="")
mat-form-field
	mat-select('matInput'="",placeholder="Select filter type", name="filterTypes",(change)="onFilterTypeChange($event)",'required'="")
		mat-option(*ngFor="let type of filterTypes", [value]="type.value")
			| {{ type.viewValue }}
mat-form-field
	mat-select('matInput'="",placeholder="Select attribute", name="attributeTypes",(change)="onAttributeTypeChange($event)",'required'="")
		mat-option(*ngFor="let type of attributeTypes", [value]="type.value")
			| {{ type.viewValue }}
mat-form-field
	input('matInput'="",placeholder="Default value",'required'="")